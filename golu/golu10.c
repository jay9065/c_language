#include<stdio.h>
int main()
{
float T,v,s,m;
printf("Enter the value of v:");
scanf("%f",&v);
printf("Enter the value of s:");
scanf("%f",&s);
printf("Enter the value of m:");
scanf("%f",&m);
printf("T=%f",((v+s)+ (1-m)*1));
return 0;
}
